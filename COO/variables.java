class Variables {
    public static void main(String[] args) {
        // Types in Java are String ("string"), int (2), float (2.99f), char ('a') and
        // boolean (true or false).
        // ONLY STRINGS USE UPPERCASE, ONLY ONE THAT IS A CLASS.
        String nome = "João";
        int num = 10;
        float num2 = 2.99f; // Use f to say it is a float.
        char letter = 'a';
        boolean condition = true;

        // To make a variable constant, use the value "final".

        final float pi = 3.14f;

        // There are other assignments, similar to C:

        int x = 2, y = 3, z = 4;

        // Or a new way, assigning the same value to multiple variables, like:

        int p, q, r;
        p = q = r = 50;

        // TYPES //

        // PRIMITIVE TYPES //
        /*
         * byte (-128 to 127), short (32,768 to 32,767), int (integers), long (big
         * integers),
         * float (fractional numbers, add a f to the end), double (big floats, use d),
         * boolean (true and false), char (letters)
         */

        // NON-PRIMITIVE TYPES //
        /*
         * String (text, words), Arrays (Data collections), Classes (classes!),
         * Interface. Reference types. These
         * refer to OBJECTS
         */

        // Casting works just like in C.

        // Operators are the same as in C.

        // Java has a MATH library with methods that work just like in C.
    }

}
